package routes

import (
	"github.com/gin-gonic/gin"
)

// TODO!
func BlocksRoute(router *gin.Engine) {
	// block := router.Group("/blocks")
	// block.GET("/:number/swaps", handlers.GetSwaps) // What swaps occurred during that specific block?
	// block.GET("/:number/assets", nil) // List all assets swapped during that specific block
}
