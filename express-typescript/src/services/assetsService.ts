import { Pool } from '../types'

export const getPoolsFromAssetId = async (assetId: Number): Promise<Pool[] | undefined>=> {
  const pools: Pool[] = []
  return pools
}

export const getVolumeFromAssetIdWithRangeTime = async (assetId: Number): Promise<Number | undefined> => {
  const volume = 0
  return volume
}
